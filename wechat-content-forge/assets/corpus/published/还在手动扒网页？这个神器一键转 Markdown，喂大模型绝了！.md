你是不是也遇过这种情况：

看到一篇好文章想喂给 AI 总结，结果复制下来全是广告、导航栏、乱码？

手动清理半小时，大模型还说「上下文太乱」…

今天给大家安利个**效率神器**—— 先用 Jina Reader API 把网页内容抽出来，再交给能执行任务的 AI 工具（比如 OpenClaw、Claude Code、Cursor、Trae），或者能配合自动化能力使用的 ChatGPT、Gemini、DeepSeek，帮你生成并运行小工具。很多公开网页都能被快速整理成更适合喂大模型的 Markdown。

---
#### 一、这个方案有多香？

1. **省掉手动清洗**：正文、标题、列表、代码块通常都能保留下来
2. **更适合喂 AI**：比直接复制网页正文干净很多，上下文噪音更少
3. **能交给 AI 代劳**：不一定要自己写脚本，直接让 OpenClaw、Claude Code、Cursor、Trae 这类工具生成并运行
4. **可扩展**：你想做成油猴脚本、浏览器书签，还是本地命令行工具都行

---

#### 二、它到底是怎么工作的？

原理其实不复杂，你可以先看这张图：

![[Pasted image 20260307231019.png]]

说白了，就是在原网页 URL 前面加一层 `r.jina.ai/`，先把乱糟糟的网页内容整理成更适合机器读取的文本，再交给 AI 去处理、总结，或者继续做成你想要的小工具。

---

#### 三、最省事的用法

1. 先确认你有一个合适的 AI 工具，比如 **OpenClaw**、**Claude Code**、**Cursor**、**Trae**，或者能配合自动化能力使用的 **ChatGPT**、**Gemini**、**DeepSeek**
2. 把你的需求直接丢给它，不用先自己手写脚本
3. 让它基于 `r.jina.ai/原网页地址` 生成一个可运行方案
4. 你只需要测试一下效果，再让它继续迭代

---

#### 四、怎么让 AI 直接帮你做？

你可以直接把下面这段需求发给 OpenClaw、Claude Code、Cursor、Trae，或者其他能配合自动化能力使用的 ChatGPT、Gemini、DeepSeek：

```text
帮我做一个网页转 Markdown 的小工具。

要求：
1. 基于 Jina Reader API，通过 https://r.jina.ai/ + 当前网页 URL 的方式抓取正文
2. 支持把结果展示出来，并能复制为 Markdown
3. 优先做成最容易运行的版本；如果适合，也可以做成 Tampermonkey 油猴脚本
4. 代码写完后，直接帮我运行或给出可执行结果
5. 如果网页无法抽取，要明确提示失败原因

先给我可运行版本，再告诉我怎么继续优化。
```

如果你想再具体一点，可以继续补一句：

“我希望它支持快捷键触发、弹窗展示、复制按钮，必要时再加下载 `.md` 文件功能。”

这样做的好处是，你不用自己从零写油猴脚本，也不用研究接口细节，直接把目标交给 AI，它来生成、运行、修正。

---

#### 五、进阶小技巧（推荐）

如果你后面真的要长期用，再去看 [jina.ai/reader](https://jina.ai/reader) 的账号和额度规则就行。无 key 可以先试；要更稳定地跑，再考虑加 key。这里要注意一点：带 key 的调用通常会进入按 token 计费体系，只是新用户一般会有试用额度，别把它理解成“永久免费无限用”。

---

#### 六、什么时候适合自己写，什么时候适合交给 AI？

如果你只是偶尔用一下，最优解往往不是自己啃脚本，而是直接把需求交给 OpenClaw、Claude Code、Cursor、Trae 这类能执行任务的 AI，或者配合 ChatGPT、Gemini、DeepSeek 这类工具一起用，让它一次性帮你搭出来。

只有在下面这些场景里，你再考虑自己维护脚本更合适：

1. 你要长期高频使用
2. 你需要固定快捷键、固定弹窗样式、固定导出格式
3. 你想把它做成团队内部工具
4. 你需要更细的异常处理和站点兼容逻辑

---

#### 结尾

有了这个组合拳，不管是收集资料、喂 AI 写摘要，还是存档技术文档，都会顺手很多。

重点不是那几十行脚本本身，而是你已经可以把“网页清洗成 Markdown”这类小工具，直接外包给 AI 去做了。

以前你要自己查接口、写代码、调报错；现在更高效的做法，是把目标讲清楚，让 OpenClaw、Claude Code、Cursor、Trae 这类工具直接帮你生成、执行、再迭代，或者让 ChatGPT、Gemini、DeepSeek 先帮你把方案和代码框架搭出来。

如果你也在折腾 AI 工作流，这种用法真的很值。
